import { HighlightPipePipe } from './highlight-pipe.pipe';

describe('HighlightPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightPipePipe();
    expect(pipe).toBeTruthy();
  });
});
